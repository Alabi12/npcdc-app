<p class="text-green-500"><%= notice %></p>

<h1 class="text-2xl font-bold mb-4">Data capturings</h1>

<div id="data_capturings">
  <% @data_capturings.each do |data_capturing| %>
    <%= render data_capturing %>
    <p class="mt-2">
      <%= link_to "Show this data capturing", data_capturing, class: "text-indigo-600 hover:underline" %>
    </p>
  <% end %>
</div>

<%= link_to "New data capturing", new_data_capturing_path, class: "text-indigo-600 hover:underline mt-4 block" %>
